from setuptools import setup

setup(
    name="cyclotron-algorithm-project",
    description="",
    install_requires=["pypubsub==4.0.3"],
    setup_requires=["pytest-runner"],
    tests_require=["pytest"]
)